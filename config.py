"""
config.py — 所有可调参数
修改这里的值来定制检测行为，无需改其他文件。
"""

# ── 截帧区域 ──────────────────────────────────────────
# None = 全屏截取；
# 指定区域示例（以像素为单位，相对于主显示器左上角）：
#   CAPTURE_REGION = (0, 0, 1920, 1080)  -> (left, top, right, bottom)
CAPTURE_REGION = None

# ── AI 检测 ───────────────────────────────────────────
# 置信度阈值：低于此值的检测结果会被丢弃（0.0~1.0）
CONFIDENCE_THRESHOLD = 0.5

# YOLOv8 模型文件名（首次运行自动下载）
# 可选：yolov8n.pt (最快) / yolov8s.pt / yolov8m.pt (最准)
MODEL_NAME = "yolov8n.pt"

# ── 覆盖层外观 ────────────────────────────────────────
# 高亮框颜色（Tkinter 颜色格式）
BOX_COLOR = "#00FF41"       # 黑客绿

# 边框宽度（像素）
BOX_WIDTH = 2

# 标签字体大小
LABEL_FONT_SIZE = 11

# FPS 计数器颜色
FPS_COLOR = "#FFFF00"       # 黄色

# FPS 计数器字体大小
FPS_FONT_SIZE = 14

# ── 性能 ──────────────────────────────────────────────
# 主循环目标帧率（实际帧率受 GPU 推理速度限制）
TARGET_FPS = 30

# ── 快捷键 ────────────────────────────────────────────
# F9  = 开/关 检测覆盖层
# ESC = 退出程序
TOGGLE_KEY = "f9"
EXIT_KEY = "esc"
